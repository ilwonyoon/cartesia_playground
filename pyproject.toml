[project]
name = "cartesia-playground"
version = "0.1.0"
description = "Playground for experimenting with the Cartesia API (TTS/STT/voices)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cartesia[websockets]>=3.0.2",
    "python-dotenv>=1.2.2",
]
